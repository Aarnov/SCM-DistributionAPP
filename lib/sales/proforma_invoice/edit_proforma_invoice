import 'package:flutter/material.dart';

class EditProformaInvoice extends StatelessWidget {
  final String proformaId;

  const EditProformaInvoice({Key? key, required this.proformaId}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(), // Empty screen
    );
  }
}
